import { Response } from 'express';
import { Responder, ResponderOptions } from './Responder';
export interface IFailData {
    [key: string]: string;
}
export declare class ApiFail implements Responder {
    static status: string;
    static code: number;
    static respond(res: Response, data?: IFailData, opt?: ResponderOptions): void;
    private data;
    meta: any;
    res: Response;
    constructor(res: Response, data?: IFailData, meta?: any);
    readonly status: string;
    readonly code: number;
    raw(): void;
    jsend(): void;
}
