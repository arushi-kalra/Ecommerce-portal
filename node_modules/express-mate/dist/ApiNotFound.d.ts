import { Response } from 'express';
import { Responder, ResponderOptions } from './Responder';
export declare class ApiNotFound implements Responder {
    static status: string;
    static code: number;
    static respond(res: Response, message?: string, opt?: ResponderOptions): void;
    private message;
    meta: any;
    res: Response;
    constructor(res: Response, message?: string, meta?: any);
    readonly status: string;
    readonly code: number;
    raw(): void;
    jsend(): void;
}
