"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var debug_1 = __importDefault(require("debug"));
var log = debug_1.default('express-mate:responder');
function isResponder(e) {
    return (e &&
        typeof e.jsend === 'function' &&
        typeof e.raw === 'function');
}
exports.isResponder = isResponder;
function triggerResponder(responder, responseFormat) {
    log('triggering responder: %s', responder.constructor.name);
    if (isResponder(responder)) {
        switch (responseFormat) {
            case 'jsend': {
                return responder.jsend();
            }
            case 'none': {
                return responder.raw();
            }
            default: {
                return responder.raw();
            }
        }
    }
}
exports.triggerResponder = triggerResponder;
