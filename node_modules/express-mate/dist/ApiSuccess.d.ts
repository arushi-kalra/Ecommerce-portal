import { Response } from 'express';
import { Responder, ResponderOptions } from './Responder';
export declare class ApiSuccess implements Responder {
    static status: string;
    static code: number;
    static respond(res: Response, data?: any, opt?: ResponderOptions): void;
    private data;
    meta: any;
    res: Response;
    constructor(res: Response, data?: any, meta?: any);
    readonly status: string;
    readonly code: number;
    raw(): Response;
    jsend(): void;
}
