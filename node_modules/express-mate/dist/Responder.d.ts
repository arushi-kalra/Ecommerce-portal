import { Response } from 'express';
import { BaseOptions, ResponseFormat } from './settings';
export declare function isResponder(e: any): e is Responder;
export interface ResponderOptions extends BaseOptions {
    meta?: any;
}
export interface Responder {
    code: number;
    status: string;
    res: Response;
    meta: any;
    raw(): void;
    jsend(): void;
}
export declare function triggerResponder(responder: Responder, responseFormat?: ResponseFormat): void;
